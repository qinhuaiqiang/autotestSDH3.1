<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "http://testng.org/testng-1.0.dtd">
<suite name="TestClass">
    <test name="TestDemo">
        <parameter name="ip01" value="${ip01}" />
        <parameter name="loginURL" value="${loginURL}" />
        <parameter name="masterIpLastByte" value="${masterIpLastByte}" />
        <parameter name="slave01IpLastByte" value="${slave01IpLastByte}" />
        <parameter name="slave02IpLastByte" value="${slave02IpLastByte}" />
        <parameter name="passWord" value="${passWord}" />
        <classes>
            <class name="TestClass01"/>
        </classes>
    </test>
</suite>
